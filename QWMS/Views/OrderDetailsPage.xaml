<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="QWMS.Views.OrderDetailsPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:model="clr-namespace:QWMS.Models"
    xmlns:viewModel="clr-namespace:QWMS.ViewModels"
    Title="NewPage1"
    x:DataType="viewModel:OrderDetailsViewModel">
    <ScrollView>
        <VerticalStackLayout>
            <!--<BoxView
                BackgroundColor="{StaticResource Primary}"
                HeightRequest="150"
                HorizontalOptions="Fill"
                VerticalOptions="Fill" />-->
            <Label
                Margin="0,0,0,8"
                FontAttributes="Bold"
                HorizontalOptions="Center"
                Text="{Binding Order.Name}" />
            <Label
                Margin="0,0,0,8"
                FontAttributes="Bold"
                HorizontalOptions="Center"
                Text="{Binding Order.Contractor}" />
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>